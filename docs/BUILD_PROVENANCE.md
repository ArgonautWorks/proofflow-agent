# Build-window provenance

The All Things Agentic Hackathon build window opened on August 3, 2026. ProofFlow Agent did not exist before that window.

Repository evidence:

- repository created: August 5, 2026;
- first commit: `6d26e0f` (`feat: launch ProofFlow submission agent`);
- all application, agent, tests, diagrams, and documentation were authored in this repository during the contest window; and
- no pre-existing project code was copied into the build.

The repository's public commit history is the canonical provenance record: <https://github.com/ArgonautWorks/proofflow-agent/commits/main/>.

Standard open-source dependencies are declared in `package.json` and locked in `package-lock.json`.
